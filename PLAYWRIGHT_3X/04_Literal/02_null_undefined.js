
let value = null;
console.log(value);

//undefined =A variable that has been declared but has not yet been assigned a value.
//  It is a primitive value automatically assigned to variables that have just been declared, 
// or to formal arguments for which there are no actual arguments.

let undefinedValue;
console.log(undefinedValue);

//null = A special value that represents "no value" or "no object".
//  It is an assignment value. 
// It can be assigned to a variable as a representation of no value.

let nullValue = null;
console.log(nullValue);

//difference between null and undefined is that null is an assignment value,
//  while undefined is a type itself (undefined).
